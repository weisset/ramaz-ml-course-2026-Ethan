[project]
name = "hw00-python-bootcamp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-json-report>=1.5",
    "pytest-timeout>=2.3",
    "ruff>=0.4",
    "ty>=0.0.1a0",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
markers = [
    "lists: list manipulation functions",
    "dicts: dictionary functions",
    "sets: set operations",
    "hof: higher-order functions",
    "classes: object-oriented programming",
    "songs: song-dataset aggregation & ranking (file I/O, Counter, ranking classes)",
]
addopts = "-v --tb=short --strict-markers"
timeout = 30
